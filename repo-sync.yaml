apiVersion: configsync.gke.io/v1alpha1
kind: RepoSync
metadata:
  name: repo-sync
  namespace: test
  annotations:
    configsync.gke.io/cluster-name-selector: support-190,support-191
spec:
  git:
    repo: https://github.com/config-sync-examples/mono-wet.git
    revision: HEAD
    branch: main
    dir: "configsync/namespace-5"
    auth: token
    secretRef:
      name: git-creds


